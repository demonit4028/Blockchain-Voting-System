# Participant 4 — TODO: implement VotePool class
# Constructor: __init__(candidates: List[str])
# Methods: add_vote(), already_voted_in_chain(), get_pending_votes(),
#          remove_votes(), count(), get_log()
