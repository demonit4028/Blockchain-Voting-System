# Participant 3 — TODO: implement create_routes(node) -> Blueprint
# Endpoints: GET /ping, /status, /peers, /chain, /votes/pending, /log
#            POST /peers/register, /blocks/receive, /votes/submit, /votes/receive
